export const config = {
  /**
   * Куда открывать экран In.Plan (SPEC §4.8). По умолчанию — новая вкладка.
   *
   * Решение владельца от 31.08.2026 (process-map-6ap). До него здесь стояло
   * '_top' с автоматическим фолбэком, и фолбэк оказался ЕДИНСТВЕННЫМ путём:
   * проба доступности верхнего окна читала top.document, а он для чужого
   * origin бросает SecurityError всегда. Карта живёт на GitHub Pages, вики —
   * на своём домене, то есть origin разные по построению.
   *
   * Переключать на '_top' можно, только если карту положили на тот же origin,
   * что и вики, И проверили на стенде: автоматики больше нет, фолбэка тоже, а
   * при sandbox без allow-top-navigation браузер откажет ТИХО, без исключения.
   */
  linkTarget: '_blank' as '_top' | '_blank',
  compactHeight: 640,
};
